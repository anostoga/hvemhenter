# Kopier denne filen til .env.local og fyll inn faktiske verdier.
# URL til backend-API-et (Fly.io i produksjon, localhost lokalt).
# Server-only (ingen NEXT_PUBLIC_-prefiks) — brukes av next.config.mjs sin
# rewrites()-proxy, aldri eksponert til nettleseren.
BACKEND_URL=http://localhost:8080
